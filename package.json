{
  "name": "redflags",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "nodemon server.js && cd client && npm run build",
    "build": "cd client && npm install && npm run build",
    "server": "nodemon server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^4.5.3",
    "custom-env": "^2.0.1",
    "express": "^4.17.1",
    "react-bootstrap": "^1.4.0",
    "socket.io": "^2.3.0"
  }
}
